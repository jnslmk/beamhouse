{
  "name": "beamhouse",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": {
    "packages": [
      "app",
      "bridge",
      "packages/*"
    ],
    "catalog": {
      "@types/bun": "1.3.14",
      "typescript": "5.9.2"
    }
  },
  "scripts": {
    "build": "bun run --cwd app build",
    "dev": "bun run --cwd bridge dev",
    "format": "prettier --write app bridge packages tests .github README.md package.json tsconfig.json tsconfig.tests.json eslint.config.mjs .prettierrc .lintstagedrc",
    "format:check": "prettier --check app bridge packages tests .github README.md package.json tsconfig.json tsconfig.tests.json eslint.config.mjs .prettierrc .lintstagedrc",
    "lint": "eslint .",
    "start": "bun run --cwd bridge start",
    "test": "bun test --timeout 20000",
    "typecheck": "bun run --cwd packages/wire typecheck && bun run --cwd bridge typecheck && bun run --cwd app typecheck && tsc -p tsconfig.tests.json",
    "prepare": "husky && bun run build"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/bun": "catalog:",
    "eslint": "10.9.1",
    "husky": "9.1.7",
    "lint-staged": "17.4.1",
    "playwright": "1.62.1",
    "prettier": "3.9.6",
    "sacn": "4.6.2",
    "typescript": "catalog:",
    "typescript-eslint": "8.69.0"
  }
}
